package cred

const Description string = "work with credentials"

const DescriptionList string = "list all credentials"

const DescriptionGet string = "retrieve the value of a credential"

const DescriptionSet string = "create a credential or update the value of an existing credential"

const DescriptionRemove string = "remove a credential (delete)"

const ShowKeysFlagUsage string = `
		print credential values in "key=value" format when using "get" command instead of just "value"
		default: [false]
		`